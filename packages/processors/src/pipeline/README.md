# Processors Pipeline

This package holds reusable placeholders for the fixed V1 knowledge-processing order:

- `score`
- `dedupe`
- `summarize`
- `classify`
- `group`

The package supports the `Knowledge Layer`. It does not replace the `Knowledge` domain boundary.
